# Jonathan's Toolbox

[[`Toolbox Site`](https://toolbox.jonathanlawhh.com/) [`My Website`](https://jonathanlawhh.com/)]

Just a collection of tools, that I use myself. Nothing too fancy, the UI was built entirely using Google Antigravity.

## Setup

Make sure to install dependencies:

```bash
# npm
npm install
```

## Development Server

Start the development server on `http://localhost:3000`:

```bash
# npm
npm run dev
```

## Tools

### Line Joiner

A utility to join multiple lines of text with custom delimiters, prefixes, suffixes, sorting, and more. Features include:

- Delimiter selection
- Output prefix/suffix
- Per‑line prefix/suffix
- Trim whitespace / remove empty lines
- Sort lines alphabetically
- Statistics (total, duplicate, unique, empty lines)

### JSON Mapping

Transform JSON data using a schema mapping. Features include:

- Generate schema from sample JSON
- Map keys between source and target schemas
